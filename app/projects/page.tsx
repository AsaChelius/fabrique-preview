import { ProjectsRoute } from "@/components/three/sculpture/projects-route";

export const metadata = {
  title: "FABRIQUE — projects",
  description: "Projects placeholder — three circles for now.",
};

export default function ProjectsPage() {
  return <ProjectsRoute />;
}
