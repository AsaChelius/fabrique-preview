import { SculptureRoute } from "@/components/three/sculpture/sculpture-route";

export const metadata = {
  title: "FABRIQUE — title",
  description:
    "Anamorphic sculpture prototype — thousands of suspended metal shards resolve into the wordmark from one angle.",
};

export default function TitlePage() {
  return <SculptureRoute />;
}
