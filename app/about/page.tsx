import { AboutRoute } from "@/components/about/about-route";

export const metadata = {
  title: "FABRIQUE — About",
  description: "Two pilots, one workshop. The story behind FABRIQUE.",
};

export default function AboutPage() {
  return <AboutRoute />;
}
